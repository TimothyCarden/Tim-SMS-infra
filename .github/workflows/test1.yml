name: Test
run-name: "Test pipeline"
on:
  create:
    branches: ["refs/heads/release/*","refs/heads/origin/release/*"]
  push:
    branches: ["develop"]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to ${{ steps.prompt.outputs.branch_name }} on ${{ steps.prompt.outputs.environment }}
        run: |
          echo "Hello world"

